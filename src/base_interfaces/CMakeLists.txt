#source

set(SIMULATION_INTERFACE_SOURCE
${CMAKE_CURRENT_SOURCE_DIR}/DominionInterface.cpp

)

if(WITH_CARLA)
	set(SIMULATION_INTERFACE_SOURCE ${SIMULATION_INTERFACE_SOURCE} ${CMAKE_CURRENT_SOURCE_DIR}/CARLAInterface.cpp)
endif(WITH_CARLA)

set (COSIMA_SOURCE ${COSIMA_SOURCE} ${SIMULATION_INTERFACE_SOURCE} PARENT_SCOPE)